.bundle
.env
.env.*
docker-compose.*
docker/Dockerfile
docker/dockerfiles
log
storage
public/system
tmp
.codeclimate.yml
public/packs
node_modules
vendor/bundle
.DS_Store
*.swp
*~

# Fora do contexto de build (só upload, nada disso entra na imagem final):
# .git pesa ~400 MB e o SHA entra por --build-arg GIT_SHA; spec/ já era apagado após o
# precompile; tests/, docs/ e .github/ não são lidos pelo build nem em runtime.
.git
spec
tests
docs
.github
